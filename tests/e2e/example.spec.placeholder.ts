// Playwright placeholder. Real e2e specs are added once auth + data-backed
// pages exist (Phase 03+). Renamed to *.spec.ts and run via `playwright test`.
//
// import { test, expect } from "@playwright/test";
//
// test("landing page loads", async ({ page }) => {
//   await page.goto("http://localhost:3000");
//   await expect(page.getByRole("heading", { name: "AI YouTube Factory" })).toBeVisible();
// });
export {};
