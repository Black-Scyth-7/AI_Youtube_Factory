# Loads every dashboard JSON from the mounted directory on start-up.
apiVersion: 1

providers:
  - name: ai-youtube-factory
    type: file
    folder: AI YouTube Factory
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
      foldersFromFilesStructure: false
