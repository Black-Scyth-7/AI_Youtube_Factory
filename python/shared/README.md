# ayf-shared

Shared, provider-neutral Python constants, enums, and utilities used by the
backend and worker. No business logic — only definitions that must stay
identical across services (job statuses, the content-pipeline stage order,
service names).
